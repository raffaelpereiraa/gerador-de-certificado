//Serve para remover todos os imports que não estou utilizando.
Comando: ng generate @angular/core:cleanup-unused-imports
//

//Foi instalado a biblioteca uuid (npm install uuid)-  serve para gerar Identificadores Únicos Universais (Universally Unique Identifiers - UUIDs). Esses identificadores são sequências de 128 bits, geralmente representadas por 32 caracteres hexadecimais (divididos por hifens), projetados para serem únicos em todos os sistemas do mundo, sem a necessidade de uma coordenação central.  
